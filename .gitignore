# Dependencies
node_modules/
.pnp
.pnp.js

# Builds
.next/
out/
build/
dist/

# Env
.env
.env*
!.env.example

# Client design source, ~70MB of binaries (PDF, DOCX, PPTX, PSD/AI, photos).
# Kept local, not in git. See CLAUDE.md "Asset library" for what's in it and
# scripts/build-assets.sh to regenerate public/ from it.
Cosmos Assets/
**/Cosmos Assets/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
.gstack/
tsconfig.tsbuildinfo
.vercel

# iCloud duplicate artifacts. Syncing this folder leaves byte-identical copies
# named "<name> 2.<ext>" beside the original; 22 of them (21 menu photos and a
# whole script) had been committed and were shipping in the deploy, referenced
# by nothing. Ignored so they cannot come back the same way.
* 2.*
