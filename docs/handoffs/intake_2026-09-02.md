# Intake handoff — Cosmos Burger, 2026-09-02

## Status
PASS

## Summary
Read the blueprint facts, LobsterLab (the sibling pattern), and the K13 genome/starter-kit.
Wrote `PROJECT_BRIEF.md` and `SCOPE.md` (with Lorena's open questions batched in). Hand-built
the Next.js 15 + Tailwind v4 P0 scaffold (never `create-next-app`, the directory wasn't empty):
brand tokens, fonts (Poppins body, Archivo variable stand-in for Horizon), Lenis smooth scroll +
scroll-reveal wiring, security-headers config, `.gitignore`, `CLAUDE.md` + `.claude/` (agents,
Lessons, settings with the GStack berth), starter-kit scripts/docs copied in. Wrote
`scripts/build-assets.sh` and ran it: derives `public/` from the gitignored `Cosmos Assets/`
(hero + 4 photo bands as webp, 5 best-seller cutouts + 6 plate cutouts trimmed to alpha bbox,
logo.svg, pattern.png). Claimed dev port 9157 and wrote it back to the War Room's
`starter-kit/CONVENTIONS.md`. Proved the scaffold: `npm install`, `npm run build` (clean), and a
live dev-server curl on :9157 returning 200.

## Files
- `PROJECT_BRIEF.md`, `SCOPE.md`
- `package.json`, `tsconfig.json`, `next.config.mjs`, `postcss.config.mjs`, `next-env.d.ts`
- `app/layout.tsx`, `app/page.tsx`, `app/globals.css`
- `components/SmoothScroll.tsx`, `lib/content.ts` (stub: `site`, `nav`)
- `.gitignore`, `README.md`
- `scripts/build-assets.sh` (new, Cosmos-specific) and `scripts/{hm,new-branch,polish-check,save,setup-identity,ship}.sh` (copied from starter-kit)
- `CLAUDE.md`, `CONVENTIONS.md`, `HAIL_MARY.md` (repo root copies)
- `.claude/Lessons.md`, `.claude/settings.json`, `.claude/agents/*.md`
- `tasks/todo.md`
- `public/photos/*.webp`, `public/menu/best-sellers/*.png`, `public/menu/plates/*.png`,
  `public/brand/{logo.svg,pattern.png}` (generated, gitignore-exempt, committed)
- Edited (not committed): `/Users/k13/Desktop/PROJECTS/K13-WarRoom/starter-kit/CONVENTIONS.md`
  (port registry row for 9157)

## Risks
- **Menu content, ordering URLs, contact info, Oceanside status, and the Horizon licence
  question are all blocked on Lorena** — batched in `SCOPE.md` "Open questions" and
  `tasks/todo.md` "Blocked on client". None of these block P1 (the site builds around honest
  "coming soon" states per house rule).
- **Archivo needed a build fix during P0.** `next/font`'s `axes` option only works with
  `weight: "variable"`, not a fixed `"900"`. Fixed by loading Archivo as `weight: "variable"` and
  pinning `font-weight: 900` + `font-variation-settings: "wdth" 125` in a `.font-display` CSS
  utility (`app/globals.css`) instead. Confirmed with a clean `npm run build`.
- **No `.env` needed for this build** (no forms, no backend) — if that scope changes, revisit
  before adding secrets.

## Next
frontend-engineer (Natalia) — build P1, the full one-pager section by section from
`PROJECT_BRIEF.md`'s page map and `docs/intake/BLUEPRINT_FACTS_2026-09-02.md`, filling
`lib/content.ts` as she goes. **brand-dna-designer (Valentina) is running in parallel** and owns
`public/styleguide.html` + `docs/handoffs/design_2026-09-02.md` exclusively; this run did not
touch either path.

## Human gate
None. All open items are client-facing questions for Lorena, batched in `SCOPE.md` and
`tasks/todo.md`, not decisions for Kazim. No commits or branches were created (per rule); the
repo is still on `main` with zero commits, ready for the next agent's branch.
